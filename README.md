# Rust Projects

A collection of projects completed while learning Rust.

## Projects

- `two-way-map` — A generic two-way map, which represents a bidirectional map backed by two BTreeMaps.

Each directory is an independent Rust project with its own `Cargo.toml`.
